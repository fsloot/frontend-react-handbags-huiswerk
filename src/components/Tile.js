import React from 'react';
// import ourStory from "../assets/our_story.png";

function Tile(props) {
    return (
        <section>
            {props.children}
        </section>
    )
}

export default Tile;

